# 项目状态与验证证据

更新日期：2026-09-12

| 模块 | 状态 | 当前证据 | 范围说明 |
|---|---|---|---|
| procfs 解析器 | 已实现 | 双采样、PID 复用测试与 Ubuntu CI 在线采样 | 负责 OS 侧进程指标 |
| 节点与进程数据模型 | 已实现 | 单元测试 | 心跳年龄由外部输入 |
| 关联规则 | 原型完成 | 固定 JSONL 轨迹与回归测试 | 尚未接入 ROS 2 在线事件 |
| 恢复决策 | dry-run 原型 | 冷却时间和预算测试 | 不执行重启命令 |
| ROS 2 在线适配 | 规划中 | 无在线采集数据 | 计划接入 `rclpy` 与 `/diagnostics` |
| systemd 执行器 | 规划中 | 无 | 将作为独立监督适配层 |
| 树莓派实验 | 规划中 | 无 | 后续记录检测延迟与监控开销 |

## 下一阶段验收目标

1. 在 Linux 与树莓派环境保存真实 procfs 采样记录。
2. 实现最小 `rclpy` 心跳订阅器。
3. 建立节点名称、PID、启动时间和可执行文件的显式注册。
4. 增加 CPU 压力、回调阻塞与进程退出故障注入。
5. 保存环境、阈值、原始 Finding 和重复实验统计。
