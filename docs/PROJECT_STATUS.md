# Project status and evidence

更新日期：2026-09-11

| 模块 | 状态 | 当前证据 | 简历中可否写“完成” |
|---|---|---|---|
| procfs解析器 | 已有基线 | 单元测试；Ubuntu CI在线采样步骤 | 可以写“实现基线” |
| 跨层数据模型 | 已有基线 | 单元测试 | 可以写“建立模型” |
| 关联规则 | 已有基线 | 固定JSONL轨迹与回归测试 | 可以写“实现规则原型” |
| 恢复决策 | dry-run基线 | 冷却和预算测试 | 只能写“设计策略”，不能写“完成自动恢复” |
| ROS 2在线接入 | 待实现 | 暂无在线证据 | 不可写完成 |
| systemd执行器 | 待实现 | 暂无 | 不可写完成 |
| 树莓派实验 | 待实施 | 暂无本仓库实验记录 | 不可写实测结果 |
| AI运行时功能 | 不在范围内 | 无模型调用 | 不可声称接入AI模型 |

## 下一步最小闭环

1. 在Linux或树莓派上运行 `sample --pid self`；
2. 逐行解释 `procfs.py` 的字段和单位；
3. 自主增加一条规则及相应测试；
4. 实现最小 `rclpy` 心跳订阅器；
5. 保存真实运行环境和原始日志，再统计性能指标。

